# 视频字幕生成器使用文档

## 1. 启动程序

双击：

`启动双语字幕工具.vbs`

不要通过命令行运行。若启动失败，程序目录会生成 `startup_error.log`。

程序会以无控制台方式运行；转录分段调用的 FFmpeg/FFprobe 也会隐藏黑色命令窗口。界面会自动适配 Windows 显示缩放以减少文字和控件发糊。修改后需完全关闭并重新打开程序才会生效。

首次使用或移动项目目录后，可双击 `启动环境配置助手.vbs`，或在主程序顶部点击“环境配置助手”。它是独立图形界面，会检查路径、GPU、FFmpeg、Whisper-GPT、模型目录和 API 连通性。

## 1.1 首次配置与环境检测

环境配置助手会按以下顺序检测：

- 项目当前目录：项目移动后仍会以自身所在目录为根目录。
- Whisper-GPT：选择包含 `scripts\transcribe.py` 的文件夹。
- Python 环境：检查 `venv\Scripts\python.exe`。
- FFmpeg / FFprobe：可以手动选择 `ffmpeg.exe`；缺少时点击“FFmpeg 下载页”。
- GPU / CUDA：通过当前 Whisper-GPT 环境检查 CTranslate2 支持的 CUDA 计算类型；不可用时主程序自动使用 CPU int8。
- 模型目录：提示模型缓存路径和是否可能需要首次下载。
- 中转站 API：可输入 Base URL、Key，测试 `/models` 连通性；Key 不会写入项目配置文件。

点击“保存配置”会在程序目录写入 `project_config.json`，保存 Whisper-GPT、FFmpeg、Base URL、模型和启动 Python 的路径。主程序启动时优先使用它；路径失效时会回退到项目附近的候选目录，因此项目移动后只需重新运行检测并保存即可。

如果保存路径和旧路径都失效，启动脚本还会尝试使用系统 `pyw -3` 打开界面；此时请优先启动环境配置助手，重新选择 Whisper-GPT 文件夹并保存。

“安装 Whisper-GPT 依赖”不会自动执行；只有用户点击并在确认框中同意后，才会执行 `pip install -r requirements.txt`。

如果 large-v3 模型尚未下载，可点击“下载 large-v3 模型”。该操作同样会先显示确认框，并使用当前选择的 Whisper-GPT Python 环境下载到其 `models` 目录。

## 2. 选择文件夹

- 媒体文件夹：放置需要处理的视频或音频。
- 输出文件夹：保存字幕、转录文本和日志。
- 扫描子文件夹：勾选后递归扫描，并在输出目录保留原相对目录结构。

支持常见的 MP4、MKV、AVI、MOV、WebM、MP3、WAV、M4A、AAC、FLAC、OGG、Opus 等格式。

## 3. 选择识别设置

### 内容预设

- 普通电影：普通剧情对白。
- 动画片：加强口癖、拟声词、喊叫和语气词。
- 动作片：保留短促对白、低声、喊叫、喘息和无线电通话。
- NSFW：使用适合低声内容的识别参数。是否过滤语气声由独立的“字幕内容”决定。
- 网课：侧重专业术语、数字、公式和完整论述。
- 互联网短视频：侧重快速口播、网络用语、口头禅和中英混说。

### 语言模式

纯单语言适用于同一个文件夹基本只有一种语言：

- 可以直接选择中文、英语、日语等。
- 选择“自动检测并整批锁定”时，程序根据第一段确定语言，后续全部沿用。

中外文混合适用于一句或一个视频中出现多种语言：

- 选择“中外文混合（不锁定语言）”。
- 原文语言选项不会强制锁定。
- Whisper 使用 multilingual 模式识别代码切换内容。
- 混合语言模式会把 Whisper 分段缩短为 5 分钟，每段重新检测语言；纯语言模式保持 30 分钟分段。

### 字幕内容

- `对白优先（过滤拟声词）`：默认推荐，适用于所有内容预设。写入原声版前删除纯语气声、拟声词、损坏字符和连续重复，只保留有实际语义的对白。
- `完整转录（包含语气声）`：提高非语言发声的保留倾向，适合确实需要记录嗯、啊、呼吸声等内容的场景。

### 设备

- `auto`：优先 GPU，失败后弹窗并使用 CPU。
- `GPU`：强制使用 CUDA/float16，失败则任务报错。
- `CPU`：使用 CPU/int8，速度通常较慢。

### 性能模式

- `均衡（推荐）`：large-v3、beam 3、宽松 VAD。保留低声对白前后缓冲，同时跳过明显静音。
- `精确（较慢）`：beam 5、关闭 VAD，完整扫描所有声音。
- `快速`：beam 1、较积极 VAD，速度优先。

自动语言检测会先通过 VAD 跳过视频开头的空白，再综合最多 5 个有效语音窗口。低于 0.75 的结果不会锁定，后续分段继续检测。

### 时间轴防拖影

软件默认启用 Whisper 词级时间对齐，并检查每一条字幕的最大可读时长。如果模型把一句短对白错误地写成数十秒或数分钟，程序会截断其显示结束时间，避免空白画面一直停留上一句字幕。已有原声版在重新翻译时会另外生成 `_原声版_时间轴已修复.srt`，不会覆盖原文件。

对于字幕提前出现的问题，程序还会允许首句使用完整 30 秒解码窗口的真实时间位置、关闭跨长静音区的上一段文本继承，并跳过可能的静音区幻觉字幕。该策略只在重新转录后生效；它优先保证时间同步，极少数无把握的字幕可能被省略。

Whisper 进度速度会随内容变化：静音和清晰对白处理较快，密集、重叠或模糊声音需要更多解码时间，因此瞬时 ETA 上下跳动属于正常现象。

## 4. 勾选字幕版本

可以任意组合，至少选择一个：

- 原声版：`视频名_原声版.srt`
- 中外版：`视频名_中外版.srt`
- 目标语言版：例如 `视频名_中文版.srt`

中文视频只需要原文字幕时，可以只勾选“原声版”，此时不需要 API。

中外版的格式如下：

```text
1
00:00:01,000 --> 00:00:03,000
Original dialogue
目标语言译文
```

生成中外版时，程序会自动处理 PotPlayer 的双语显示限制：不足约 1.2 秒的连续短字幕会合并或延长，过宽的双语句子会按时间拆成较短片段。这样可以减少暂停、跳转时字幕闪失，以及译文换行后把第一行原文顶出画面的情况。

## 5. 配置翻译 API

只有勾选中外版或目标语言版时才需要 API。

## 6. 整理为同名播放文件夹

在“需要生成的字幕版本”区域勾选“完成后复制视频、转录文本和已勾选字幕到同名文件夹（不移动原文件）”。勾选后，字幕和转录文本会从一开始直接写入输出目录（递归扫描时保留相对目录结构）中的视频同名文件夹；只有视频本身会从输入目录复制一份过来，例如：

```text
输出\episode\
  episode.mp4
  episode_转录文本.txt
  episode_原声版.srt
  episode_中外版.srt
```

输出根目录不会再保留第二份字幕或转录文本。只会放入界面中已勾选的字幕版本。视频始终是复制，原始媒体目录不会被移动或删除；相同视频已存在且未变化时会跳过复制。旧版程序已经写在输出根目录的本程序文件会迁移到同名文件夹，避免重复；即使字幕已生成、只想整理文件夹，也可以勾选此项直接运行，无需填写 API。

### Base URL

可填写：

- 官方或中转站根地址：`https://example.com`
- 带版本地址：`https://example.com/v1`
- 完整 Responses 地址：`https://example.com/v1/responses`
- 完整 Chat 地址：`https://example.com/v1/chat/completions`

### API Key

粘贴服务商提供的 Key。默认只保存在当前程序内存中。若勾选“保存 Key（本机明文）”，会保存到：

`%LOCALAPPDATA%\WhisperBilingualSubtitleGUI\settings.json`

### 获取模型

点击“获取模型”，程序尝试 `/v1/models` 和 `/models`，成功后可从模型下拉框选择。

### 接口格式

推荐选择“自动（优先 Responses）”。程序依次兼容 Responses API 和 Chat Completions。只有服务商明确要求时才手动指定。

### 测试 API

程序会同时按时长、字符数和字幕条数分批，单批最多 150 条。遇到读取超时、中转站主动断开或 JSON 截断时会递归拆分；当小块已经只剩 10 条或更少仍然失败时，立即整块使用原文回填，不再继续拆分和等待。日志会列出降级的字幕序号。每个成功小批次都会立即写入输出目录下的 `.translation_cache` 断点缓存；任务失败或程序重启后，会只补译缺失条目。缓存不包含 API Key，且只有在原字幕、目标语言和模型都一致时才会复用。

点击后发送一条极小请求。成功时显示实际使用的协议和接口；失败时显示原因及修改建议。

## 7. 翻译分段和并发

- 每段分钟：默认 60。每小时字幕作为一个上下文块上传。
- 字符保护：即使不足一小时，超过约 24000 字符也会提前切块。
- 并发数：范围 1–5，默认 3。
- 超时秒数：默认 300，可设置为 60–900。大型分段超时后会自动二分并继续翻译。
- 模型输出 JSON 不完整、ID 缺失或没有 JSON 时，也会自动二分，避免反复提交同一个过大的分段。

建议：

- 普通中转站先使用并发 3。
- 中转站明确支持 5 并发时可设置为 5。
- 出现 HTTP 429 时降低到 1 或 2。
- 长上下文模型可保留 60 分钟；上下文较小的模型可改成 20–30 分钟。

进度条显示全部转录文件和全部翻译分段的整体进度。并发请求即使完成顺序不同，最终字幕仍按原时间顺序排列。

## 8. 已有字幕和重新翻译

如果发现 `视频名_原声版.srt`：

- 不再运行 Whisper。
- 如果需要翻译版，会询问是否重新翻译。
- 选择“是”：重新调用 API，更新所勾选的翻译字幕。
- 选择“否”：保留已有翻译，只处理新视频。
- 选择“取消”：不开始任务。

旧版本的 `视频名_字幕.srt` 会自动转换为原声版使用。

如果原声版本身识别错误，可勾选“重新转录已有原声版”。程序还会检查损坏字符、内容唯一率、异常重复和媒体时长覆盖率；质量不合格时不会覆盖已有原声版，也不会调用 API，而会保存为 `_质量不合格.srt` 供检查。“低质量也强制翻译”只用于确认原文可用但被误判的特殊情况。

## 9. PotPlayer 使用

1. 使用 PotPlayer 打开视频。
2. 把需要的 `.srt` 拖入播放窗口。
3. 或在 PotPlayer 的字幕菜单中手动选择字幕。

字幕采用 UTF-8 BOM，中文、日文、韩文等通常不会乱码。

## 10. 常见错误

### GPU 不可用

选择 `auto`，程序会自动切换 CPU。若希望修复 GPU，请检查 NVIDIA 驱动、CUDA DLL 和 CTranslate2 兼容性。

### HTTP 401

API Key 无效或过期，重新复制 Key 并检查账户权限。

### HTTP 403

可能是权限、IP 白名单、WAF 或账户分组限制。

### 模型不受支持

地址和 Key 已到达服务器，但当前账户分组没有该模型。点击“获取模型”，改选中转站实际提供的模型。

### HTTP 405

接口路径或协议不匹配。使用自动接口格式，或复制服务商提供的完整端点。

### HTTP 429

请求过快、额度不足或并发过高。降低并发并检查余额。

### Upstream access forbidden

说明请求已经到达中转站，但它配置的上游账户、模型渠道或内容策略拒绝了请求。该错误不是 Base URL 拼错，程序会立即停止而不再无效重试。需要在中转站切换渠道/分组、改用其他模型或服务商，或联系中转站管理员。

### 翻译连续失败

原声版仍会保留。修正 API 后再次运行，选择重新翻译，无需重复 Whisper。

### 翻译结果条目数量或 ID 不匹配 / 没有 JSON 数组

这表示模型遗漏了部分字幕 ID、返回内容被截断，或者没有遵守 JSON 输出要求。当前版本会自动把失败分段二分后继续翻译，最小可拆到单条，并在合并时恢复原始字幕顺序。

日志会显示期望条目数、实际条目数、收到的 ID 和模型响应开头。如果拆到单条后仍失败，通常是模型拒答或该模型不适合结构化输出。建议：

- 把“每段分钟”调到 15–30。
- 并发数先设为 2。
- 换用支持结构化 JSON 输出的快速模型。
- 已有原声版时选择重新翻译，不需要重新运行 Whisper。

### 环境噪声被识别成文字

如果选择“完整转录”，NSFW 和动作片可能产生较多非语言字幕。只需要有效对白时，把“字幕内容”改为“对白优先”。

## 11. 推荐配置

普通单语言影片：

- 语言模式：纯单语言
- 原文语言：直接指定，或自动检测并整批锁定
- 内容预设：普通电影
- 每段分钟：60
- 并发：3

中英混合短视频：

- 语言模式：中外文混合
- 内容预设：互联网短视频
- 每段分钟：20–30
- 并发：3

需要保留语气声的内容：

- 语言模式：纯单语言
- 内容预设：NSFW
- 字幕内容：完整转录（包含语气声）
- 原文语言：尽量直接指定
- 每段分钟：60
- 并发：2–3
