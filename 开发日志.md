# 视频字幕生成器开发日志（发布版）

## 发布包说明

本发布包已移除用户视频、音频、字幕、转录文本、日志、翻译缓存、API Key、历史配置和本机绝对路径。

## 当前功能

- Tkinter 图形界面与 Windows 高 DPI 适配。
- FFmpeg/FFprobe 无黑框调用。
- GPU 优先、失败自动回退 CPU int8。
- Whisper 原声转录，支持普通电影、动画片、动作片、NSFW、网课与短视频预设。
- 原声版、中外版、目标语言版字幕。
- OpenAI-compatible Responses / Chat Completions API 兼容、模型列表和错误诊断。
- API 超时拆分、翻译缓存和原文降级回填。
- PotPlayer 时间轴优化、词级时间对齐和异常时间段保护。
- 可选同名播放文件夹整理。
- 独立的首次配置与环境检测 GUI。

## 发布版默认值

- 内容预设：普通电影
- 字幕内容：完整转录（包含语气声）
- 性能模式：均衡（推荐）
- 设备：GPU
- API 配置：空白

## 验证

- Python 语法检查。
- 主 GUI 与环境配置 GUI 可创建。
- 子进程无窗口参数、路径发现和配置读取逻辑检查。

真实模型下载、转录和 API 调用需要由使用者在自己的环境中执行。
